class Payment():

    def receive_bid_from_user(user_id, amount):
        # check if user has already placed bid bid = bid - prev_bid
        # freeze money
        print("Succesfully received the bid from the user")
        return True
